{
  "name": "fictional-parakeet",
  "version": "1.0.0",
  "description": "Fictional-parakeet is a mock application with GraphQL, React and Typescript",
  "author": "David Zarandi",
  "private": true,
  "license": "BSD-3-Clause",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "server:build": "npm run build --workspace=server",
    "server:start:dev": "npm run start:dev --workspace=server",
    "server:start:prod": "npm run start:prod --workspace=server",
    "server:migrate:seed": "npm run migrate:seed --workspace=server",
    "server:migrate:up": "npm run migrate:up --workspace=server",
    "client:build": "npm run build --workspace=client",
    "client:start:dev": "npm run start:dev --workspace=client"
  }
}
